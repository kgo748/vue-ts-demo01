暂只看了 P27，P29。

创建环境：
    node.v16.14.0, @vue/cli 5.0.8, npm.v.8.3.1;
创建项目：
    cmd>vue create vue-ts-demo01	win11如果创建项目失败，就以管理员的身份打开cmd去创建；
    选择 Default ([Vue 3] babel, eslint)
### 安装插件库：
vuex：
    cmd>npm install vuex@next --save
